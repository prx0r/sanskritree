namespace Sanskritree

axiom Identity : {α : Type} → α → α → Prop
axiom Qualification : {α : Type} → α → α → Prop
axiom Manifestation : {α : Type} → α → α → Prop
axiom Dependence : {α : Type} → α → α → Prop
axiom Causation : {α : Type} → α → α → Prop
axiom Sequence : {α : Type} → α → α → Prop
axiom Prescription : {α : Type} → α → α → Prop
axiom Prohibition : {α : Type} → α → α → Prop
axiom Relation : {α : Type} → String → α → α → Prop

end Sanskritree
